<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android" >

    <!-- NOTE: EditTextPreference accepts EditText attributes. -->
    <!-- NOTE: EditTextPreference's summary should be set to its value by the activity code. -->
    

    <!--
         NOTE: Hide buttons to simplify the UI. Users can touch outside the dialog to
         dismiss it.
    -->
    <!-- NOTE: ListPreference's summary should be set to its value by the activity code. -->
    <ListPreference
        android:entries="@array/pref_size_titles"
        android:entryValues="@array/pref_size_values"
        android:key="size"
        android:negativeButtonText="@null"
        android:positiveButtonText="@null"
        android:title="@string/pref_size_display_name" />
    
    <EditTextPreference android:capitalize="words" android:inputType="textCapWords" android:key="site" android:maxLines="1" android:selectAllOnFocus="true" android:singleLine="true" android:title="@string/pref_site_display_name" />
    <ListPreference android:entries="@array/pref_color_titles" android:key="color" android:entryValues="@array/pref_color_values" android:title="@string/pref_color_display_name"/>
    <ListPreference android:title="@string/pref_type_display_name" android:entryValues="@array/pref_type_values" android:entries="@array/pref_type_titles" android:key="type"/>
    
    

</PreferenceScreen>
